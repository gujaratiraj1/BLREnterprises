// GitHub Pages configuration. Enable only after the backend is deployed.
// The Node server generates this file from its environment when hosting the site.
window.BLR_CONFIG = Object.freeze({
  assistantEnabled: false,
  assistantEndpoint: '',
  formspreeFormId: ''
});
