# Set these on your backend host. Never put the API key in frontend files.
OPENAI_API_KEY=
ASSISTANT_ENABLED=false
PORT=3000
# Exact browser origin, without a path or trailing slash.
ALLOWED_ORIGIN=http://localhost:3000
# Optional public Formspree form identifier (not a secret).
FORMSPREE_FORM_ID=
